# REST Web Server

## 1. .env file
Clone the .env.template and create the .env file and add your own configurations

## 2. Execute docker command
Execute the next command to execute the PostgreSQL database.
```shell
docker compose up -d
```
## 2. Execute dev environment
Execute the next command for the dev environment.
```shell
npm run dev
```